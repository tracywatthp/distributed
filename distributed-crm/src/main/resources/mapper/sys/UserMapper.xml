<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mountains.distributed.mapper.UserMapper">


    <insert id="addUser">

    </insert>
    <select id="findById" resultType="com.mountains.distributed.entity.crm.sys.User" parameterType="long">
      select * from user where id = #{id}
    </select>
    <select id="findAll" resultType="com.mountains.distributed.entity.crm.sys.User">
      select * from user
    </select>
</mapper>
