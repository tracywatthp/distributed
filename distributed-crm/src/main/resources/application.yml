server:
  port: 8001

########################## mybatis-plus 配置 ##########################
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml, classpath:mapper/**/*.xml

########################## mybatis-plus 配置 ##########################

spring:
  profiles:
    active: mysql
  application:
    name: distributed-crm